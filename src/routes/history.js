// import createHistory from 'history/createHashHistory'; // better for individual view development
import createHistory from 'history/createBrowserHistory'; // better for application flow development
const history = createHistory();

export default history;
